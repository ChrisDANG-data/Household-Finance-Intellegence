[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
# Monorepo: only rebuild when LangGraph files change (set same path in Railway UI watch patterns)
watchPatterns = ["**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
